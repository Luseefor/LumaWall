---
description: Git author and prose identity for LumaWall
alwaysApply: true
---

# Authorship

- Git author and committer must be `Luseefor <sapanakosansar18@gmail.com>`.
- Never commit as `cursor-agent`, `Cursor`, `Cursor Agent`, or any tool bot name.
- Do not run `git config` to change identity. Pass `GIT_AUTHOR_NAME`, `GIT_AUTHOR_EMAIL`, `GIT_COMMITTER_NAME`, and `GIT_COMMITTER_EMAIL` on the commit command when needed.
- Do not put agent branding, "Generated by…", or AI marketing copy in source, README, or commit messages.
- Keep comments to non-obvious why only. No phase narration. No Wallspace/Phosphene comparison essays in code.
